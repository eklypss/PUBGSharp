namespace PUBGSharp.Helpers
{
    /* Defines all the names of seasons used in the API.
     TODO: Refactor the wrapper so it uses this instead of the enum version, that way new seasons
     added to the API won't break the wrapper.*/

    public static class SeasonNames
    {
        public const string EarlyAccessSeasonOne = "2017-pre1";

        public const string EarlyAccessSeasonTwo = "2017-pre2";
    }
}